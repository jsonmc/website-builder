extends layout.pug

block title
  title= "JsonMC: " + actor.name
block content
  h2#actor-name= actor.name
  h3 Movies
  .movie-list
    each movie in actorMovies
      div
        a(href=movie.url)= movie.name + ' (' + movie.year + ')'